import { Select } from 'bits-ui';
import Content from './content.svelte';
import Item from './item.svelte';
import Trigger from './trigger.svelte';

const Root = Select.Root;

export default { Root, Content, Item, Trigger };
